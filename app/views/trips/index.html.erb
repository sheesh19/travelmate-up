<!-- Search bar -->
<%= render partial: 'shared/search/search_main', 
    locals: {
        search_title: 'Find trip inspiration',
        path: trips_path,
        search_placeholder: 'Search trips',
        elements: @trips
    }
%>

<%= render 'trips/index/featured_trips', trips: @trips %>

<%= render 'trips/index/latest_trips', trips: @trips %>