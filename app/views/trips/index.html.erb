<!-- Search bar -->
<%= render partial: 'shared/search/search_main', 
    locals: {
        search_title: 'Find trip inspiration',
        path: trips_path,
        search_placeholder: 'Search trips',
        elements: @trips
    }
%>

<%= render 'featured_trips', trips: @trips %>

<%= render 'latest_trips', trips: @trips %>