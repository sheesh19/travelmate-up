<div class="map">
    <%= render 'shared/components/map' %>
</div>

<!-- TITLE & USER BLOCKS -->
<%= render 'trip_banner', trip: @trip %>

<%= render 'trip_events', trip: @trip %>

<div class="page-bottom-spacer"></div>