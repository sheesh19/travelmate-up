<!-- LOCATIONS -->
<div class="almost-white-background">

  <%# <div>
      <button class="button is-checked" data-sort-by="original-order">original order</button>
      <button class="button js-sort-btn" data-sort-by="name">name</button>
  </div> %>

  <div class="page-container-home">
    <div class="featured-title-home d-flex justify-content-between align-items-center">
      <h2>DESTINATION DISCOVERY</h2>
    </div>
    <div  data-controller="infinite-scroll" data-action="scroll@window->infinite-scroll#scroll">
      <!-- Card Carousel -->

      <div class="card__grid js-grid" data-infinite-scroll-target="entries">
        <%= render 'locations' %>
      </div>

      <div class="mt-5" data-infinite-scroll-target="pagination">
        <%== pagy_nav(@pagy) %>
      </div>

    </div>
  </div>
</div>
