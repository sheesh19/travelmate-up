<!-- Search bar -->
<%= render partial: 'shared/search/search_main', 
    locals: {
        search_title: 'Discover destinations',
        path: locations_path,
        search_placeholder: 'Search locations',
        elements: @locations,
        class_type: ''
    }
%>

<%# <div class="js-fade"> %>
<div>
  <!-- Featured locations -->
  <div class="almost-white-background pt-4">
    <%= render 'locations/by_location/featured_location' %>
  </div>

  <!-- ALL LOCATIONS -->
  <div>

    <%= render 'locations/by_location/non_featured_locations' %>
  </div>
  
</div>
