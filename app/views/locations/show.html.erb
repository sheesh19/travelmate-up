<%= render partial: 'shared/components/background', 
    locals: { 
        element: @location,
        title: @location.city
    } 
%>

<%= render partial: 'shared/cards/card_carousel', locals: { element: @location } %>

<%#= render 'locations/by_location/activities_by_location', location: @location
%>

<%= render 'locations/by_location/upcoming_events', location: @location %>


<%= render partial: 'shared/events/all_events_bottom', 
    locals: {
        events: @location.events,
        title: "ALL EVENTS IN #{@location.city.upcase}"
    }
%>

